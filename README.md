# surveyAppJS

A simple JavaScript web app for collecting health survey data with skip logic.

Includes a grid question example ("cigarettes per day by age bin") rendered as a table in the survey flow.
